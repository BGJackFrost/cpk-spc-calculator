# ============================================
# SPC/CPK Calculator - Environment Configuration
# ============================================
# Copy this file to .env and update the values
# ============================================

# ============================================
# DATABASE CONFIGURATION (Required)
# ============================================
# Format: mysql://username:password@host:port/database
DATABASE_URL=mysql://spc_app:YourStrongPassword@localhost:3306/spc_calculator

# ============================================
# APPLICATION SETTINGS (Required)
# ============================================
NODE_ENV=production
PORT=3000

# ============================================
# OFFLINE MODE CONFIGURATION
# ============================================
# Set to "true" for offline deployment (no internet required)
OFFLINE_MODE=true

# Authentication mode: "local" for offline, "manus" for online
AUTH_MODE=local

# Storage mode: "local" for offline, "s3" for online
STORAGE_MODE=local

# LLM mode: "disabled" for offline, "cloud" for online
LLM_MODE=disabled

# Local storage path for file uploads (when STORAGE_MODE=local)
LOCAL_STORAGE_PATH=./uploads

# ============================================
# AUTHENTICATION (Required)
# ============================================
# Generate a secure random string (at least 32 characters)
# PowerShell: [Convert]::ToBase64String((1..48 | ForEach-Object { Get-Random -Maximum 256 }) -as [byte[]])
# Linux: openssl rand -base64 48
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long

# ============================================
# REALTIME FEATURES (Optional)
# ============================================
# Server-Sent Events for realtime notifications
SSE_ENABLED=false

# WebSocket for realtime updates
WEBSOCKET_ENABLED=false

# ============================================
# RATE LIMITING (Optional)
# ============================================
# Enable rate limiting for API protection
RATE_LIMIT_ENABLED=false

# Redis URL for distributed rate limiting (optional)
# REDIS_URL=redis://localhost:6379

# ============================================
# SMTP EMAIL CONFIGURATION (Optional)
# ============================================
# Required if you want to send email notifications
# SMTP_HOST=smtp.company.local
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=spc@company.local
# SMTP_PASS=your_email_password
# SMTP_FROM_EMAIL=spc@company.local
# SMTP_FROM_NAME=SPC Calculator System

# ============================================
# LICENSE SERVER (Optional)
# ============================================
# Enable license checking
LICENSE_CHECK_ENABLED=false

# License server URL (if using external license server)
# LICENSE_SERVER_URL=http://license.company.local:3001

# ============================================
# APP BRANDING (Optional)
# ============================================
VITE_APP_TITLE=SPC/CPK Calculator
VITE_APP_LOGO=/logo.svg
